import styled from "styled-components";
import {
  background,
  border,
  color,
  flexbox,
  layout,
  position,
  shadow,
  space,
  typography,
  grid,
} from 'styled-system';

export const Box = styled.div`
  ${color}
  ${layout}
  ${space}
  ${typography}
  ${flexbox}
  ${background}
  ${border}
  ${position}
  ${shadow}
  ${grid}
`;